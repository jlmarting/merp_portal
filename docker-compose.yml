services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=${SECRET_KEY:-merp-dev-secret-key}
    volumes:
      - ./backend/merp.db:/app/merp.db
    restart: unless-stopped
